import styled from 'styled-components';

const S = {
  layout: styled.section`
    width: 100%;
    max-width: 600px;
    z-index: 10;
    position: absolute;
    bottom: 0;
  `,
};

export default S;
